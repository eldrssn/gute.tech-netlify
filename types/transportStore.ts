export type ListOptionsItemData = {
  title: string;
  slug: string;
};

export type BrandSlug = {
  brandSlug: string;
};

export type YearsSlugs = {
  brandSlug: string;
  modelSlug: string;
};

export type EnginesSlugs = {
  brandSlug: string;
  modelSlug: string;
  yearSlug: string;
};
