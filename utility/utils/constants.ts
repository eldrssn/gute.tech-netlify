const CATALOG_QUERY_DEFAULT = 'page=1&order=byPopularDown';
const TIMEOUT_DELAY = 250;

enum TotalBoxRedirectUrls {
  ORDER = '/order',
  PAYMENT = '/payment',
}

export { CATALOG_QUERY_DEFAULT, TIMEOUT_DELAY, TotalBoxRedirectUrls };
